<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="MostWanted.ListWanted"
                 xmlns:model="clr-namespace:MostWanted.Model"
    xmlns:modelview="clr-namespace:MostWanted.ViewsModels"
    x:DataType="modelview:WantedPersonListViewModel"
             Title="ListWanted">
    <Grid ColumnSpacing="1" RowSpacing="1" Padding="5">
        <Grid.RowDefinitions>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="auto"/>

            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <!-- Header -->
   





        <!-- RefreshView and CollectionView -->
        <RefreshView Grid.Row="4"
                    Command="{Binding GetWantedPersonListCommand}"
                    IsRefreshing="{Binding IsRefreshing}">

            <CollectionView ItemsSource="{Binding WantedPersons}" SelectionMode="None">
                <CollectionView.ItemTemplate>

                    <DataTemplate x:DataType="model:WantedPerson">

                        <!-- Single root: Border -->
                        <Border HeightRequest="70" Padding="5" Margin="5">

                            <!-- Grid for row layout -->
                            <Grid ColumnSpacing="10">

                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto"/>
                                    <!-- Id -->
                                    <ColumnDefinition Width="2*"/>
                                    <!-- Name -->
                                    <ColumnDefinition Width="3*"/>
                                    <!-- Type -->
                                    <ColumnDefinition Width="Auto"/>
                                    <!-- Update -->
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="Auto"/>
                                    <!-- Delete -->
                                </Grid.ColumnDefinitions>

                                <!-- Tap gesture on row -->
                                <Grid.GestureRecognizers>
                                    <TapGestureRecognizer 
                                       Command="{Binding Source={RelativeSource AncestorType={x:Type modelview:WantedPersonListViewModel}}, Path=GetWantedPersonDetailsCommand}" 
                                       CommandParameter="{Binding Id}" />
                                </Grid.GestureRecognizers>

                                <!-- Data -->
                                <Label Grid.Column="0" Text="{Binding Id}" VerticalOptions="Center"/>
                                <Label Grid.Column="1" Text="{Binding Name}" VerticalOptions="Center"/>
                                <!--<Label Grid.Column="2" Text="{Binding Description}" VerticalOptions="Center"/>-->
                                <Label Grid.Column="2" Text="{Binding Type}" VerticalOptions="Center"/>

                                <!-- Buttons -->
                           

                                <Button Grid.Column="4"
                                       Text="Delete"
                                       Margin="5"
                                       HeightRequest="30"
                                       WidthRequest="70"
                                       BackgroundColor="Red"
                                       TextColor="White"
                                       Command="{Binding Source={RelativeSource AncestorType={x:Type modelview:WantedPersonListViewModel}}, Path=DeletePersonCommand}"
                                       CommandParameter="{Binding Id}" />



                                <Button Grid.Column="5"
        Text="Update"
        Command="{Binding Source={RelativeSource AncestorType={x:Type modelview:WantedPersonListViewModel}}, Path=UpdateWantedPersonCommand}"
        CommandParameter="{Binding .}" />

                            </Grid>

                        </Border>

                    </DataTemplate>

                </CollectionView.ItemTemplate>
            </CollectionView>

        </RefreshView>

    </Grid>
</ContentPage>